node_modules/
.pnpm-store/
.turbo/
dist/
build/
.next/
out/

.env
.env.local
.env.*.local
!.env.example

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

coverage/
*.tsbuildinfo

# Orca local runtime state (memory is versioned, scratch is not)
.orca/tmp/
.orca/cache/
# 사람마다 다른 선택입니다 (GitHub 응원 건너뛰기 기록) — 공유하지 않습니다
.orca/state/
wiki/memory/short-term/*.local.md

# Local tooling state — session ids and absolute paths, never shared
.omc/

# Generated images land in the repo, but codex scratch does not
.codex-tmp/

# 개인 작업 로그 — 공유하지 않습니다
project-docs/devlog.md

# 전달용 임시 파일 — 포트폴리오 본문 원고 (제출 전 삭제)
scripts/포트폴리오_본문_5건.md
