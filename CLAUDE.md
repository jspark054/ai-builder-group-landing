# CLAUDE.md

Claude Code 가 이 저장소에서 작업할 때 따르는 지침입니다.

> **이 문서에는 규칙만 적는다.** 진행 상황·화면별 결정·카피는 `project-docs/` 의 해당 문서에 적는다.
> 규칙을 추가·수정할 때는 같은 주제가 이미 다른 절에 있는지 먼저 확인한다. 한 주제는 한 곳에만 적는다.

> 세션 시작 시 SessionStart 훅이 `scripts/load-context.sh` 를 실행해
> `project-docs/devlog.md` 의 **NOW 블록**과 문서 우선순위를 주입합니다.
> 이 문서는 그 위에 얹히는 **행동 규칙**입니다.

---

## 프로젝트

**AI 빌더그룹 랜딩페이지** — 주식회사 똑똑한개발자 발주, 2조 수행.
검증된 바이브코더를 조직이 보증해 외주 프로젝트로 연결하는 것이 사이트의 목적이다.
**교육이 아니라 외주 수주가 핵심 목적**이다 (회의록 260812).

```
apps/web          공개 화면 (Next.js 16, :3000)
apps/admin        관리 화면 (Next.js 16, :3001)
packages/supabase 클라이언트 · 스토리지 · 마이그레이션
project-docs/     기획 · 확정 문서 · 진행 상태
```

범위는 **화면 19종**이다 (확정문서 21종 − P-07 교육 목록 · A-04 교육 관리).
본 범위에 없는 화면은 만들지 않는다.

---

## 문서

작업 시작 전 필요한 문서만 골라 읽는다. 전부 읽지 않는다.

### 충돌 시 우선순위

| 순위 | 문서 | 관할 |
|:---:|---|---|
| 1 | `project-docs/260812_서대표미팅_회의록.md` | **클라이언트 직접 결정. 모든 문서에 우선** |
| 2 | `project-docs/확정문서(260814)/260811_4_결정시트.md` | 팀 결정 · tie-breaker |
| 3 | `project-docs/확정문서(260814)/` | 화면 구조 · 기능 · 데이터 · 정책 |
| 4 | `project-docs/260814_구현결정_박진선.md` | 구현 단계 판단 |
| 5 | `project-docs/기획안_*.md` · `project-docs/PRD_*.md` | 카피 · 문구 |

### 영역별 기준

| 영역 | 기준 |
|---|---|
| 화면 구조 · 기능 · 데이터 · 정책 | `확정문서(260814)/` |
| 색 · 폰트 · 모서리 | `apps/web/app/ai-builder-design-tokens.css` · `ai-builder-tailwind-theme.css` (변경 불가 — 클라이언트 채택 시안2) |
| 여백 · 배경 리듬 · 섹션 라벨 · 인터랙션 배치 | `project-docs/디자인규칙_AI빌더그룹_랜딩페이지.md` |
| **카피 · 문구** | **`기획안_*.md` · `PRD_*.md` 만** |

### 확정문서(260814) 안내

- `4_02_화면목록(IA)` — 화면 ID · 경로 · 렌더링. 범위 기준선
- `4_05_화면설계` — 화면별 섹션 도면. 가장 자주 참조
- `4_03_기능명세` — 화면별 동작 정의
- `4_04_정책정의` — POL 규칙. **POL-11① 은 반려 사유이므로 필수 확인**
- `4_06_데이터모델` — 스키마 · RLS · 인덱스
- `4_01_요구사항정의서` / `4_01-1_상세(근거)` — 범위 경계와 근거
- `4_결정시트` — 팀 결정 이력

### 진행 기록

`project-docs/HISTORY.md` — 08-22 이전 날짜별 작업 기록. 비공개 파일(`.gitignore`).
결정은 `devlog.md` 의 「## 결정」 절을 먼저 본다.

### 참조 금지

`project-docs/archive/` — 구버전 산출물과 타 팀원 카피 후보.
**카피 소스가 아니다.** 와이어프레임 v0.3 · 기획안/PRD 구버전도 여기 있다.

### 카피 기준 (이 절에만 존재한다)

- 모든 문구는 `기획안_AI빌더그룹_랜딩페이지.md` · `PRD_AI빌더그룹_랜딩페이지.md` 기준.
- 기획-2안은 팀장 작업본이며 확정 사항이 아니다 (결정시트 `I-7`). 교체 대상이다.
- 기획안·PRD 에 해당 문구가 없으면 **지어내지 말고 사용자에게 먼저 묻는다.**
- 단, 아래는 예외로 변경하지 않는다: 확정 소구점 3건, `POL-01` · `POL-09` · `POL-11①`

번호 체계 — 팀 확정 문서는 `REQ-` / `FN-` / `POL-`, 내 PRD 는 `FR-`.
구조·기능에서 확정 문서와 다르게 가야 하면 **먼저 알리고 승인을 받는다.**

---

## 하드 룰

위반하면 작업을 중단하고 사용자에게 알린다.

### 1. 스타일

- **Tailwind 기본 색 클래스 금지** (`text-gray-500`, `bg-white` 등).
  기본 팔레트가 비활성화되어 있어 **에러 없이 조용히 무시된다.**
- **그림자 클래스 전면 금지** (`shadow-*`).
- 색·폰트·모서리는 디자인 토큰 CSS 변수만 쓴다. 하드코딩 금지.
- 상세는 `project-docs/디자인규칙_AI빌더그룹_랜딩페이지.md`.

### 2. 모션은 채우는 게 아니라 비우는 것

- `POL-11①-2`(절제 — 적용하지 않는 영역을 의도적으로 남긴다)는 **반려 사유**다.
  전 섹션에 등장 애니메이션을 깔면 탈락한다.
- P-01 에서 **모션을 비워둘 구간은 1(히어로) · 6(교육·조직) · 8(문의)** 로 확정돼 있다.
- 인접 섹션은 서로 다른 인터랙션을 쓴다 (`FN-P01-02`).
- 모션 값은 토큰(`--duration-base` · `--ease-out`)만 쓴다. 임의 수치 금지.

### 3. 이미지

- 이미지는 `apps/web/public/images/` 에 직접 배치한다.
- **코드로 SVG 를 그려 이미지를 대신하지 않는다.**
- 인사이트 본문과 커버 이미지는 **발주사가 작성**한다. 생성하지 않는다.

### 4. 데이터

- 콘텐츠의 진실 공급원은 **Supabase** 다. 파일 기반 콘텐츠를 도입하지 않는다.
- 스키마·RLS·인덱스는 `4_06_데이터모델` 을 따른다. 임의로 컬럼을 늘리지 않는다.
- 파생 값은 저장하지 않는다. 단, `insight.cover_image_url` 은 명시된 예외이며
  본문 저장 시 함께 갱신한다.

### 5. 공개 렌더링

- 공개 화면은 **서버에서 콘텐츠가 포함된 HTML 을 반환**해야 한다 (`REQ-N-001`).
  CSR 단독은 공개 영역에서 쓰지 않는다. 검색 색인과 답변엔진 인용이 성립하지 않는다.
- 관리 화면(`A-nn`)은 CSR · `noindex`.

### 6. 인사이트 라우팅

- `before` · `process` · `people` 3개는 **카테고리 전용 예약어**다.
- 예약어를 먼저 카테고리로 해석하고, 그 외를 글 슬러그로 해석한다.
- 슬러그 검증은 **DB CHECK 와 A-07 입력 검증 양쪽에** 둔다.
- 공개 조건은 `status = 'published'` **이고** `published_at` 이 있을 때다. 그 외는 404.

---

## 작업 전에

1. `project-docs/devlog.md` 의 **NOW 블록**을 확인한다. 이미 결정된 것을 다시 묻지 않는다.
2. 해당 화면의 `4_05_화면설계` 절과 `4_03_기능명세` 를 연다.
3. 카피가 필요하면 `기획안` · `PRD` 에서 찾는다. 없으면 묻는다.

## 작업 후에

```bash
pnpm typecheck       # 필수
pnpm build           # 앱을 건드렸다면
```

통과 못 한 상태로 "완료"라고 말하지 않는다. 실패했으면 출력과 함께 보고한다.

- 상태가 바뀌었으면 `project-docs/devlog.md` 의 **NOW 블록을 덮어쓴다.** append 하지 않는다.
- 결정을 내렸으면 `project-docs/devlog.md` 아래쪽 「## 결정」 절에 `D-` 번호로 append 하고,
  NOW 블록의 「결정 색인」 표에도 한 줄 추가한다.
  **되돌릴 지점(전체 경로 또는 커밋 해시)을 반드시 적는다.**
  확정문서와 다르게 갔으면 ⚠ 와 조항 번호를 달고, 「미결」 표에도 승인 요청 행을 만든다.
- 그날 한 일은 기록하지 않는다. git log 가 한다.
  긴 세션 기록이 꼭 필요하면 `project-docs/HISTORY.md` 에 append 한다.
- 판단 근거를 남길 일이 있으면 `project-docs/260814_구현결정_박진선.md` 에 append 한다.
  「## 결정」 절이 결정 자체를 담고, 이 문서는 근거가 길어 본문에 다 못 담을 때 쓴다.
  그 경우 D- 항목에서 이 문서의 절을 가리킨다.

---

## 코드 규칙

- `strict: true`, `noUncheckedIndexedAccess: true`. `any` 금지.
- 외부 입력(폼 · 환경 변수 · API 응답)은 스키마로 검증한다.
- **서버 컴포넌트가 기본.** `'use client'` 는 상호작용이 실제로 필요할 때만.
- 폼은 서버 액션(`app/actions.ts`). 폼 라이브러리를 새로 도입하지 않는다.
- `params` / `searchParams` 는 Promise 다. `await` 할 것.
- 네이티브 `<select>` 금지. `components/Select.tsx`(Radix)를 쓴다.
- **슬러그는 자연어 한글을 쓴다** (`REQ-N-013`). **발행 후 변경 금지.**
- 커밋은 Conventional Commits.

---

## 소통

- **한국어로 답한다.** 코드 주석과 커밋 메시지는 영어 혼용 가능.
- 결론 먼저. 옵션을 나열하기보다 추천안을 제시한다.
- **확인하지 않은 것을 확인했다고 말하지 않는다.** 검증 명령의 실제 출력으로 뒷받침한다.
