-- 문의 폼 주소를 「빌더그룹」 폼으로 정정한다 (8/22).
--
-- 0003 이 심어 둔 값은 `JKqzYjCHCH` 였고, 플러그에서 그 폼의 이름은
-- **"AI 교육 및 컨설팅"** 이다. 교육은 이 사이트의 범위가 아니다
-- (화면 19종 — P-07 교육 목록 제외). 문의가 교육 폼으로 들어가면 리드가 샌다.
--
-- 이 사이트가 써야 할 폼은 `Y0iItGK46B` = **"빌더그룹"** 이다.
-- 필드 구성 — 개인정보 수집동의 · 프로젝트 이름 · 회사명 · 이름 · 이메일 ·
-- 연락처 · 예산 · 프로젝트 내용. 예산 항목은 플러그 기존 구성이며 우리가
-- 추가한 것이 아니다 (REQ-F-051 은 우리가 셀렉트를 덧붙이는 것을 막는 조항이다).
--
-- 0003 의 insert 문은 고치지 않는다 — 이미 적용된 마이그레이션이다.
-- 새로 만든 DB 는 0003 이 심고 이 파일이 덮으므로 결과가 같다.

update public.site_setting
   set value      = 'https://www.pluuug.com/form/Y0iItGK46B',
       updated_at = now()
 where key = 'contact_form_url';
