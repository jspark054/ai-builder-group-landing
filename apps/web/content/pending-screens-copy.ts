// 콘텐츠 대기 화면 카피 — P-10 문의 · P-11 개인정보처리방침 · P-12 인사이트 목록
//
// 세 화면 모두 **경로는 열려 있는데 내용이 아직 없는** 상태다. 헤더·푸터·CTA 가
// 이미 이 경로들을 가리키고 있어(그리고 app/sitemap.ts 가 색인까지 요청하고 있어)
// 비워 두면 방문자와 크롤러가 404 를 받는다. 그래서 최소 화면을 먼저 세운다.
//
// 문장을 화면 파일에 직접 쓰지 않고 여기 모은다 — 화면에 박힌 문구가 늘어나면
// 발주사 컨펌 대상이 어디까지인지 추적되지 않는다 (POL-13).
// 세 화면의 문구를 한 파일에 둔 이유도 같다. 셋 다 "발주사가 채운다"는 같은 성격의
// 안내문이라 교체 시점도 함께 온다.
//
// 이 파일이 지키는 제약
//   POL-13      확정 문구가 없으면 지어내지 않는다. 아래 문장은 전부 사용자 지시 문안이다
//   REQ-F-007   전환 경로는 문의 하나다. 보조 링크·버튼 문구를 만들지 않는다
//   안건 —      연락처(이메일 · 전화번호)를 만들지 않는다. 수령 전까지 화면에 두지 않는다
//   법무        개인정보처리방침 조항을 임의로 쓰지 않는다. 본문은 발주사가 작성한다
//
// **콘텐츠 수령 시 이 파일과 해당 화면의 본문 블록을 함께 지운다.** 안내문과 수명이 같다.

type PendingScreen = {
  /** h1. 줄바꿈을 자동 흘림에 맡기지 않으므로 줄 단위 배열이다 (디자인규칙 「한글 조판」) */
  readonly headingLines: readonly string[];
  /** 본문 한 문단. 줄 단위로 끊어 <br> 로 고정한다 */
  readonly bodyLines: readonly string[];
  /**
   * meta description.
   * 본문과 별개 문장이다 — `POL-06` 이 80~110자를 요구하는데 화면 본문은 그보다
   * 짧다. 본문을 늘리는 대신 메타만 따로 둔다. 두 문장이 같은 사실을 말하도록
   * 유지한다 (POL-13 — 화면에 없는 주장을 메타에 넣지 않는다).
   * 문안은 사용자 지시값이다 (8/20)
   */
  readonly metaDescription: string;
};

/**
 * P-10 문의 `/contact`.
 *
 * 폼을 직접 만들지 않는다. 발주사가 제공하는 임베드로 연결되는 화면이라
 * 지금 폼을 세우면 수령 시점에 통째로 버리게 된다.
 * 임베드 코드가 오면 **본문 블록만** 교체하면 되도록 구조를 얕게 뒀다.
 */
export const contactPendingCopy = {
  headingLines: ['프로젝트 상담'],
  bodyLines: [
    '문의 폼은 발주사 제공 임베드로 연결됩니다.',
    '연동 준비가 끝나는 대로 이 화면에서 바로 상담을 접수할 수 있습니다.',
  ],
  metaDescription:
    '프로젝트 상담 문의 화면입니다. 문의 폼은 발주사 제공 임베드로 연결되며, 연동이 준비되는 대로 이 화면에서 바로 상담을 접수하실 수 있습니다.',
} as const satisfies PendingScreen;

/**
 * P-11 개인정보처리방침 `/privacy`.
 *
 * 법적 문서다. 조항을 대신 쓰지 않는다 — 사실과 다른 처리방침을 게시하면
 * 문서 자체가 위험이 된다. 한 문단만 두고 발주사 확정본을 기다린다.
 */
export const privacyPendingCopy = {
  headingLines: ['개인정보처리방침'],
  bodyLines: ['개인정보처리방침 본문은 발주사가 작성하며, 확정 후 이 화면에 게시됩니다.'],
  metaDescription:
    'AI 빌더 그룹의 개인정보처리방침 화면입니다. 개인정보 수집 항목과 이용 목적, 보관 기간을 담은 본문은 발주사가 작성하며, 내용이 확정되는 대로 이 화면에 게시됩니다.',
} as const satisfies PendingScreen;

/**
 * P-12 인사이트 목록 `/insights`.
 *
 * 목록을 만들지 않는다 — 글이 0건이라 빈 목록이 되고, POL-02 는 빈 요소를
 * 렌더하지 않는다. 카테고리 예약어(before · process · people) 라우팅은
 * 이번 범위가 아니다. `/insights` 한 경로만 연다.
 */
export const insightsPendingCopy = {
  headingLines: ['인사이트'],
  bodyLines: ['인사이트 글은 발주사가 작성합니다. 발행되는 대로 이 목록에 표시됩니다.'],
  metaDescription:
    'AI 빌더 그룹의 인사이트 목록입니다. 프로젝트를 맡기기 전에 알아두면 좋은 이야기와 진행 방식, 빌더 이야기를 담은 글이 발행되는 대로 이 목록에 표시됩니다.',
} as const satisfies PendingScreen;
