// P-03 포트폴리오 목록 카피 상수
//
// 이 파일은 카피만 담는다. 표현·레이아웃은 page.tsx 가 갖는다 (p09-copy.ts 와 같은 규약).
// 문장을 이 파일 밖에서 새로 쓰지 않는다 — 화면에 박힌 문구가 늘어나면
// 발주사 컨펌 대상이 어디까지인지 추적되지 않는다 (POL-13).
//
// 출처
//   H1 · 인트로 · 빈 상태  **사용자 지시 문안(8/19)**.
//     기획안 · PRD 에 P-03 목록 카피가 없어 지어내지 않고 지시값을 그대로 쓴다
//     (CLAUDE.md 「카피 기준」). 기능명세 §4.2 의 인트로 문안은 기획-2안이며
//     결정시트 `I-7` 로 교체 대상이라 채택하지 않았다.
//   화면명 「포트폴리오」  화면목록(IA) §3.1 · 화면설계 §5.2 도면
//
// 이 파일이 지키는 제약
//   FN-P03-02  상단에 키워드 대응 소개 문단을 둔다.
//              인트로 첫 문장 앞머리의 「AI 홈페이지 제작, AI 앱 개발」이 그 키워드다.
//              3단 구조로 바꾸면서 한 번 빠졌다가 복원했다 (8/20).
//              **키워드를 메타에만 두지 않는다** — 화면에 없는 문장을 검색 결과에
//              내보내지 않는다. 화면(인트로)과 메타 양쪽 앞머리에 함께 둔다
//   POL-06     meta_title 40자 이내 · 핵심 키워드 앞 15자 이내
//              meta_description 80~110자 → 아래 `metaDescription` 이 맡는다.
//              **화면 카피(intro)와 분리했다 (8/20)** — h1 이 화면 이름으로 짧아지면서
//              세 줄 어느 것도 80자가 되지 않는다. 헤더에 80자짜리 문단을 두면
//              헤더가 본문처럼 읽힌다
//   POL-01     실적 수치(건수 · 기간 · 비율)를 쓰지 않는다. 이 화면에 숫자가 없다
//   POL-02     공개 0건이어도 페이지는 남기고 emptyState 를 표시한다.
//              P-01 섹션 4 는 섹션을 통째로 숨기는 반대 처리다 (화면설계 §5.1 표)

export const p03Copy = {
  /**
   * 헤더 3단 (8/20). h1 은 화면 이름이고 주장은 중간 줄이 맡는다.
   * 종전에는 h1 이 「어디까지 만들었는지 공개합니다」였고 화면설계 §5.2 도면
   * (`▓▓▓ 포트폴리오`)과 어긋나 있었다. 그 문장을 tagline 으로 내리면서 해소됐다.
   */
  header: {
    /** 자동 흘림에 맡기지 않고 직접 끊는다 (디자인규칙 「한글 조판」). 지금은 한 줄이다 */
    headingLines: ['포트폴리오'],
    /** 중간 줄. 종전 h1 문안이다 */
    tagline: '어디까지 만들었는지 공개합니다',
    /**
     * 두 문장을 한 문자열로 둔다 — PageHeader 는 `<p>` 하나를 그리고 줄바꿈은
     * 조판에 맡긴다 (P-05 와 같은 규약).
     *
     * 첫 문장 앞머리가 `FN-P03-02` 의 「키워드 대응」이다 (8/20 복원).
     * 3단으로 바꾸면서 둘째 문장만 남아 키워드가 빠져 있었다.
     * **앞머리를 다른 문장으로 밀지 않는다** — P-03 은 "바이브코딩 포트폴리오" 류
     * 검색 착지 페이지이고(§4.2 목적), 키워드가 뒤로 가면 그 목적이 약해진다.
     */
    subtitle:
      'AI 홈페이지 제작, AI 앱 개발 프로젝트입니다. 프로젝트마다 구현 범위와 담당 영역을 확인하실 수 있습니다.',
  },

  /** 검색 결과에 걸리는 화면명. 레이아웃 template 이 뒤에 사이트명을 붙인다 */
  metaTitle: '포트폴리오',

  /**
   * 검색 결과 본문. **화면에 나오지 않는다** — POL-06 의 80~110자를 채우는 자리다.
   *
   * 화면의 세 줄(h1 · tagline · subtitle)을 재구성해 만든 것이고 새 사실을 넣지
   * 않았다. 종전 값에 있던 「주식회사 똑똑한개발자가 수행한」은 화면에서 빠졌으므로
   * 여기에도 두지 않는다 — 검색 결과에만 있고 화면에는 없는 문장이 된다.
   * (그 사실은 목록 상단 PlaceholderNotice 가 임시로 갖고 있으나 제출 전 삭제 대상이다)
   *
   * 앞머리는 인트로와 같은 키워드다 (`FN-P03-02`). 길이를 채우는 것보다 무엇을
   * 앞에 두는지가 노출을 정한다 (POL-06 본문).
   *
   * 93자. 줄이면 POL-06 하한(80자)을, 늘리면 상한(110자)을 깬다.
   */
  metaDescription:
    'AI 홈페이지 제작, AI 앱 개발 프로젝트를 어디까지 만들었는지 공개하는 포트폴리오입니다. 프로젝트마다 구현 범위와 담당 영역을 정리해 하나씩 확인하실 수 있습니다.',

  /** POL-02 — 공개 프로젝트 0건 */
  emptyState: '아직 공개된 프로젝트가 없습니다.',
} as const;
