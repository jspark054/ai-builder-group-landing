// P-04 프로젝트 상세 카피 상수
//
// 이 파일은 카피만 담는다. 표현·레이아웃은 page.tsx 가 갖는다 (p03-copy.ts 와 같은 규약).
// 문장을 이 파일 밖에서 새로 쓰지 않는다 — 화면에 박힌 문구가 늘어나면
// 발주사 컨펌 대상이 어디까지인지 추적되지 않는다 (POL-13).
//
// 출처 — **한 줄도 지어내지 않았다.** 전부 확정문서·사용자 지시값이다.
//   backToList     화면설계 §5.3 도면 「← 포트폴리오」
//   bodyHeadings   기능명세 §4.3 본문 4문항 표 · 데이터모델 §3.2 컬럼 주석
//                  (`body_what`=무엇을 만들었나 · `body_why`=왜 필요했나 ·
//                   `body_how`=어떻게 풀었나 · `body_result`=결과)
//                  4번째는 유형과 무관하게 「결과」로 통일한다. 분기하지 않는다
//                  (화면설계 §5.3 표 · 사용자 지시 8/19)
//   builders       화면설계 §5.3 표의 항목명 「담당 빌더」 · FN-P04-07 의 용어
//   metaTitleSuffix 사용자 지시 문안 (8/19)
//
// 바로가기 버튼 문구는 여기에 두지 않는다 — 등급별 문구는 이미
// content/component-copy.ts 의 `projectCardCopy.linkLabel` 에 있고 POL-03 이 출처다.
// 같은 표를 두 벌 두면 갈라진다.
//
// 이 파일이 지키는 제약
//   POL-06  meta_title 40자 이내 · 핵심 키워드가 앞 15자 이내
//           → 프로젝트명이 맨 앞에 온다. 접미사는 뒤로 민다
//   POL-01  실적 수치(건수 · 기간 · 비율)를 쓰지 않는다. 이 파일에 숫자가 없다
//   POL-13  확정 문안을 임의로 바꾸지 않는다

export const p04Copy = {
  /** 화면설계 §5.3 도면 최상단. 목록(P-03)으로 되돌아가는 유일한 경로다 */
  backToList: '포트폴리오',

  /**
   * FN-P04-03 — 본문 4문항. **그대로 h2 가 된다** (관리자 입력 구조 = 공개 화면 구조).
   * 순서를 바꾸거나 항목을 빼지 않는다. 값이 비어 있어도 제목을 숨기지 않는다 —
   * 4문항 구조 자체가 인수 기준이다 ("4개 소제목이 h2로 렌더").
   */
  bodyHeadings: {
    what: '무엇을 만들었나',
    why: '왜 필요했나',
    how: '어떻게 풀었나',
    result: '결과',
  },

  /** FN-P04-07 — 담당 빌더 영역. 도면에는 라벨이 없어 확정문서의 항목명을 그대로 쓴다 */
  builders: '담당 빌더',

  /**
   * POL-06 meta_title.
   *
   * `{프로젝트명} | AI 빌더그룹 포트폴리오` 형태다 (사용자 지시 8/19).
   * 루트 layout 의 `%s | {siteName}` 템플릿을 타면 사이트명이 두 번 붙으므로
   * page.tsx 가 `title.absolute` 로 넘긴다.
   */
  metaTitleSuffix: 'AI 빌더그룹 포트폴리오',
} as const;
