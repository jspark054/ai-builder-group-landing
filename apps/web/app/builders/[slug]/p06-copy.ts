// P-06 빌더 상세 카피 상수
//
// 이 파일은 카피만 담는다. 표현·레이아웃은 page.tsx 가 갖는다 (p03-copy.ts · p04-copy.ts 와 같은 규약).
// 문장을 이 파일 밖에서 새로 쓰지 않는다 (POL-13).
//
// 출처 — **한 줄도 지어내지 않았다.**
//   career     화면설계 §5.5 도면의 구획명 「── 이력 ──」
//   projects   기능명세 FN-P06-02 의 용어 「담당 프로젝트」
//   metaTitleSuffix  사용자 지시 문안 (8/20)
//
// 기수 배지 · 담당 건수 문구는 여기 없다 — content/component-copy.ts 의
// `builderCardCopy` 가 이미 갖고 있다. 같은 문구를 두 벌 두면 갈라진다.
//
// **여기 없는 것 두 가지.** 둘 다 쓸 문구가 확정돼 있지 않아서다.
//
//   FN-P06-09 인트로 — 기능명세 §4.5 에 인트로 문안이 실려 있으나 표에 「기획-2안
//     카피 (v3.0)」이라고 명시돼 있다. 기획-2안은 팀장 작업본이고 결정시트 `I-7`
//     로 **교체 대상**이다 (CLAUDE.md 「카피 기준」). 기획안·PRD 에 P-06 인트로
//     문구가 없어 지어내지 않고 비워 뒀다. 확정 문안을 받으면 여기 넣는다.
//
//   FN-P06-07 접기 — 이력 6개 이상일 때 초과분을 접는 <summary> 라벨이 필요한데
//     확정 문구가 없다. 지금은 상한 5개까지만 표시하고 접기를 만들지 않았다
//     (`career` 가 전 건 `[]` 이라 현재 도달하지 않는 분기다).

export const p06Copy = {
  /** 화면설계 §5.5 도면의 구획명. `career` 가 비면 이 라벨도 렌더하지 않는다 (POL-02) */
  career: '이력',

  /** FN-P06-02 의 용어. 도면에는 라벨이 없어 기능명세의 항목명을 그대로 쓴다 */
  projects: '담당 프로젝트',

  /**
   * POL-06 meta_title.
   *
   * `{표기명} | AI 빌더그룹` 형태다 (사용자 지시 8/20).
   * 루트 layout 의 `%s | {siteName}` 템플릿을 타면 사이트명이 두 번 붙으므로
   * page.tsx 가 `title.absolute` 로 넘긴다 (P-04 와 같은 처리).
   */
  metaTitleSuffix: 'AI 빌더그룹',
} as const;
