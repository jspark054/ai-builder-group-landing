// P-05 빌더 목록 카피 상수
//
// 이 파일은 카피만 담는다. 표현·레이아웃은 page.tsx 가 갖는다 (p03-copy.ts 와 같은 규약).
// 문장을 이 파일 밖에서 새로 쓰지 않는다 (POL-13).
//
// 출처
//   화면명 「빌더」   화면목록(IA) §3.1 P-05 · 화면설계 §5.4 도면 상단 · 헤더 GNB 라벨
//   인트로 · 빈 상태  **사용자 지시 문안 (8/20).**
//     화면설계 §5.4 · 기능명세 §4.4 에 P-05 인트로 문안이 없고, 기획안·PRD 에도
//     `/builders` 는 IA 표에 경로로만 나온다. 지어내지 않고 지시값을 그대로 쓴다
//     (CLAUDE.md 「카피 기준」). P-03 이 같은 경로로 채워진 것과 같은 처리다.
//     **문장을 임의로 다듬지 않는다.** 발주사 컨펌 대상이 흐려진다 (POL-13).
//
// P-01 섹션 5 의 `buildersCopy`(「만들 사람을 먼저 만나보세요」)를 끌어오지 않았다.
// 그쪽은 랜딩 섹션 카피이고 이 화면의 확정 문안이 아니다.
//
// 이 파일이 지키는 제약
//   FN-P05-04  기수별 그룹 소제목을 두지 않는다 → 그룹 라벨 문구가 아예 없다
//   POL-06     meta_title 40자 이내 · 핵심 키워드가 앞 15자 이내
//   POL-01     실적 수치(인원 수 · 건수)를 쓰지 않는다. 이 파일에 숫자가 없다

export const p05Copy = {
  header: {
    /** 자동 흘림에 맡기지 않고 직접 끊는다 (디자인규칙 「한글 조판」). 지금은 한 줄이다 */
    headingLines: ['빌더'],

    /** 중간 줄 (8/20 신설 — 네 화면 헤더 3단 통일) */
    tagline: '프로젝트를 맡을 사람을 미리 확인할 수 있습니다',

    /**
     * 제목 아래 한 문단.
     *
     * 종전 문안에서 **「어떤 교육을 이수했고」를 뺐다 (8/20).**
     * P-06 이 이수 과정을 표시하지 못하는 상태라(`course_id` 전 건 null) 헤더가
     * 화면에 없는 것을 예고하게 된다. 교육 라인업 수령 후 복원 대상이다 (안건 8).
     *
     * 어미도 함께 바꿨다 — 종전 「확인해보세요」는 명령형이라 나머지 세 화면의
     * 서술형과 어긋났다. 네 화면 헤더가 전부 「~합니다 · ~있습니다」로 끝난다.
     */
    subtitle: '어떤 프로젝트를 맡아 왔는지 프로필에서 확인하실 수 있습니다.',
  },

  /** POL-02 — 공개 빌더 0건. P-03 의 emptyState 와 같은 어법이다 */
  emptyState: '아직 공개된 빌더가 없습니다.',

  /**
   * POL-06 meta_title.
   *
   * `빌더 | AI 빌더그룹` 형태다 (사용자 지시 8/20). 루트 layout 의 `%s | {siteName}`
   * 템플릿을 타면 사이트명이 두 번 붙으므로 page.tsx 가 `title.absolute` 로 넘긴다
   * — P-04 · P-06 과 같은 처리다.
   */
  metaTitleSuffix: 'AI 빌더그룹',

  /**
   * 검색 결과 본문. **화면에 나오지 않는다** — POL-06 의 80~110자를 채우는 자리다.
   *
   * 종전에는 이 값이 없어 루트 layout 의 사이트 설명(82자)을 상속했다. 그 문장이
   * 마침 빌더를 주어로 삼고 있어 버텨 왔지만, 이 화면이 자기 헤더 세 줄을 갖게 된
   * 지금은 화면과 검색 결과가 다른 말을 하게 된다. 그래서 전용 문장을 둔다 (8/20).
   *
   * 화면의 세 줄(h1 · tagline · subtitle)을 재구성한 값이고 새 사실을 넣지 않았다.
   * 「어떤 교육을 이수했고」는 화면에서 뺀 문구이므로 여기에도 없다.
   *
   * 81자. 줄이면 POL-06 하한을 깬다.
   */
  metaDescription:
    '프로젝트를 맡을 사람을 미리 확인할 수 있는 빌더 목록입니다. 누가 어떤 프로젝트를 맡아 왔는지 빌더마다 프로필에서 하나씩 확인하실 수 있습니다.',
} as const;
